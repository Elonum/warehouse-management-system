/** Default page sizes for client-paginated tables (DataTable without serverPagination). */
export const CLIENT_TABLE_PAGE_SIZES = [10, 25, 50, 100];

/** Default page sizes for server-paginated lists (e.g. stock / useServerOffsetPagination). */
export const SERVER_TABLE_PAGE_SIZES = [25, 50, 100];
